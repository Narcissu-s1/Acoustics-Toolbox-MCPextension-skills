# 这是一个 MATLAB 项目

## MATLAB 编码规范

### 文件结构

- 每个 `.m` 文件一个函数，文件名与函数名一致
- 脚本（无 function 声明）仅用于顶层入口，不嵌套调用
- 函数文件开头：一行 H1 注释（`% 函数名  一句话说明`），然后空一行

### 函数签名

```matlab
function [out1, out2] = function_name(in1, in2, in3)
% function_name  一句话说明
%
% 参数:
%   in1  - 描述 (单位/类型)
%   in2  - 描述
%   in3  - 描述 (可选, 默认: xxx)
%
% 返回:
%   out1 - 描述
%   out2 - 描述
```

- 可选参数用 `nargin < N` 检查并设默认值，不写 `exist()` 判断
- 输入校验仅在函数边界做（外部调用入口），内部函数不做

### 命名

| 类型 | 风格 | 示例 |
|------|------|------|
| 函数名 | snake_case | `at_make_env` |
| 变量名 | 小写/缩写 | `source_depth` / `src_z` |
| 结构体字段 | 按上游约定 | `Pos.r.z`（不自行重命名） |
| 常量临时 | 大写 | `MAX_ITER` |
| 临时变量 | 含上下文 | `n_beams`, `idx_valid`，不用 `a` `b` `x` |

### 简洁原则

- 能向量化的不写 for 循环
- 同一行不塞多个语句（不用 `;` 串接不同逻辑）
- 不需要的错误处理不写 — 只在跨系统边界处（文件读写、网络请求）用 `try`/`catch`
- 不写 `clear all` / `close all`，不写 `clear` 在函数内
- 无冗余变量 — 直接作为函数输入传递

### 路径与环境

- 路径拼接统一用 `fullfile(at_root, 'Matlab', 'Bellhop')`，不用手动拼 `\` 或 `/`
- 一次性初始化用 `persistent` 防重复执行
- 输出文件写到项目目录，不污染系统临时目录

### 注释

- 注释用中文，描述 **为什么** 而非 **是什么**
- 不解释显而易见的语法（如 `% 循环`、`% 赋值`）
- 非直觉的操作（数学技巧、MATLAB 行为、workaround）必须注释

```matlab
% 正确的注释 — 解释为什么
tol = 1e-6;        % 单精度下 cond() 噪声约为 1e-7

% 不必要的注释 — 只是在重复代码
n = length(x);     % 获取 x 的长度    ← 不写
```

### 输出

- 用 `fprintf` 而非 `disp`，提供结构化信息
- 错误用 `error('描述: %s', var)`，不用 `disp` + `return`
- 不静默吞错误 — 即使返回默认值也要打印警告

### 性能

- 大数组预分配：`zeros(n, m)` 而非循环内 `[a; new]`
- 注意列优先：`reshape` / `permute` 前在注释中说明意图
- 重复计算的值提成变量（如 `length(data)`, `max(z)`），不在循环内计算

### 默认行为

- 不改 `pathdef.m` / `startup.m` / 系统级 MATLAB 配置
- 路径配置通过项目 `at_init.m` 或类似入口完成
- 使用工具箱前确认已安装（用 `ver` 或 `detect_matlab_toolboxes`），不假设所有环境都有

## 工作方式

- 在修改代码之前，先理解现有代码的结构和约定
- 改动最小化 — 只碰与当前任务相关的行
- 保持与周围代码风格一致，即使它和上述规范有出入
- 不确定时先问，不要猜测
- 每次修改一个逻辑单元，完成后确认结果再继续
